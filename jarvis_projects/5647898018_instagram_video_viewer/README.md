# Instagram Video Viewer
A Python script to send views to an Instagram video and push to GitHub.

## Installation
Run `python main.py --install` to install dependencies.

## Usage
Run `python main.py --run` to start sending views.
Run `python main.py --github` to push to GitHub.